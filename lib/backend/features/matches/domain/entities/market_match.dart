class MarketMatch {
  const MarketMatch({required this.id, required this.data});

  final String id;

  /// TODO: Ganti `data` dengan field typed setelah schema matches sudah final.
  final Map<String, dynamic> data;
}
